name: fixer
description: The Secure Bridge Terminal
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.3
  google_generative_ai: ^0.4.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
